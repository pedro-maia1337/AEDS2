#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

/*Criar a struct de Lista*/

struct typedef ListaDupla {
    struct ListaDupla prox;
    struct ListaDupla ant;
    int elemento
} no;

no criarNovaLista(int x) {
    no->prox = null;
}
